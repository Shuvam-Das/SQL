 -- INDEX:- DATABASE OBJECTION ,RETRIVE DATA
INDEX KEY -- COLUMN ON WHICH WE CREATE INDEX

 2 Types -- Clusterd and Non - Clustered

 /* 1 DB -- 1 table = only 1 cluster index --
 /* Non-Clusterd Index = Data is stored at one location,Index is at another */ Mpre than one can be added */

SELECT *FROM [DBO].[EMP]

SELECT *FROM EMP WHERE SALARY = 1000

CREATE INDEX INDEX_SALARY
ON EMP (SALARY)

--------------------------------------


